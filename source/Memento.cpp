#pragma once
/*#include <iostream>
#include <string>
#include "shapes.cpp"
#include <vector>

using namespace std;

class Memento {
protected:
	vector<shape*> estado;

public:
	Memento(const vector<shape*> & eSave) { estado = eSave; }
	vector<shape*> getSavedState() { return estado; }
};*/